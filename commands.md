Searching through old Scriptmonger screenplay .json files in the current directory for the "Rodriguez footage" scene.  

```
grep -rl "Rodriguez footage~" *.json
```
Look at the manual files to see what flags do

-r recursive search  
-l just show files with matches in stdout

```
man grep
```
